package com.salonhub.api.config;

import org.springframework.boot.test.context.TestConfiguration;

@TestConfiguration
public class TestSecurityConfig {
    // Empty configuration to override security for tests
}
